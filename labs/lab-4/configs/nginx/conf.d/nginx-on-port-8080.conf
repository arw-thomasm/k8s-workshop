server {
    listen       8080;
    location / {
        return 200 'Eureka! You imported an existing NGINX config file into your k8s cluster! Congrats!';
        add_header Content-Type text/plain;
    }
}